export declare const config: {
    readonly NODE_ENV: string;
    readonly PORT: number;
    readonly MONGODB_URI: string;
    readonly JWT_SECRET: string;
    readonly JWT_EXPIRES_IN: string;
    readonly RATE_LIMIT_WINDOW_MS: number;
    readonly RATE_LIMIT_MAX_REQUESTS: number;
    readonly FRONTEND_URL: string;
    readonly SMTP_HOST: string;
    readonly SMTP_PORT: number;
    readonly SMTP_USER: string;
    readonly SMTP_PASS: string;
    readonly ADMIN_EMAIL: string;
    readonly MAX_FILE_SIZE: number;
    readonly UPLOAD_PATH: string;
    readonly BCRYPT_ROUNDS: number;
    readonly LOG_LEVEL: string;
    readonly API_VERSION: string;
    readonly DEFAULT_PAGE_SIZE: number;
    readonly MAX_PAGE_SIZE: number;
};
export declare const validateConfig: () => void;
export declare const NODE_ENV: string, PORT: number, MONGODB_URI: string, JWT_SECRET: string, JWT_EXPIRES_IN: string, RATE_LIMIT_WINDOW_MS: number, RATE_LIMIT_MAX_REQUESTS: number, FRONTEND_URL: string, SMTP_HOST: string, SMTP_PORT: number, SMTP_USER: string, SMTP_PASS: string, ADMIN_EMAIL: string, MAX_FILE_SIZE: number, UPLOAD_PATH: string, BCRYPT_ROUNDS: number, LOG_LEVEL: string, API_VERSION: string, DEFAULT_PAGE_SIZE: number, MAX_PAGE_SIZE: number;
//# sourceMappingURL=environment.d.ts.map